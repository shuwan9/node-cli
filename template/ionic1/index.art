/**
 * {{date}} created by shuwang5456
 */

import './{{scssPath}}'

let {{moduleName}} = angular.module('{{module}}', [])

//router config
import {{routerConfig}} from './{{configPath}}'
{{moduleName}}.config({{routerConfig}})

// Controllers
import {{controller}} from './{{controllerPath}}'
{{moduleName}}.controller('{{controller}}', {{controller}})

export default {{moduleName}}
